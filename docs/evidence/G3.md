# G3 — Channel eksternal (Resend / Telegram / WhatsApp)

Tanggal: 2026-08-22. Server: `pmmiserver`.

## Status

| Channel | Status | Alasan |
|---|---|---|
| In-App (durable, source of truth) | ✅ **LIVE** — terverifikasi | Delivery `IN_APP\|SENT` nyata (16 baris, termasuk 3 notifikasi ops hari ini 2026-08-22 dari ops-monitor) |
| EMAIL (Resend) | ⛔ BLOCKED — tidak ada `RESEND_API_KEY` | Perlu key + DNS verify domain |
| TELEGRAM | ⛔ BLOCKED — tidak ada `TELEGRAM_BOT_TOKEN`/username | Perlu token @BotFather + webhook secret |
| WHATSAPP | ⛔ BLOCKED — `WHATSAPP_PROVIDER=disabled` | Perlu nomor khusus + Baileys pairing atau Meta token |

`WHATSAPP_PROVIDER=disabled`; tidak ada `RESEND_API_KEY`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_BOT_USERNAME`, `RESEND_WEBHOOK_SECRET`, `TELEGRAM_WEBHOOK_SECRET`, `META_WHATSAPP_*`, `BAILEYS_SERVICE_TOKEN` di env produksi (diverifikasi 2026-08-22).

## Temuan konfigurasi: NOTIFICATION_TRANSPORT=mock

Env produksi memakai `NOTIFICATION_TRANSPORT=mock` → `sendExternal()` mengembalikan `mock-<channel>-<uuid>` → delivery tercatat `SENT` **tanpa pengiriman nyata** (lih. `apps/worker/src/providers.ts`). Ini sesuai DEPLOYMENT.md ("Set live hanya setelah provider production siap") — **intentional sementara**, tapi perlu dicatat: delivery `EMAIL|SENT` saat ini TIDAK berarti email terkirim.

Saat satu channel live dikonfigurasi (G3 unlock):
1. isi kredensial channel di `.env` (+ webhook secrets);
2. `NOTIFICATION_TRANSPORT=live`;
3. restart worker; verifikasi `notification_deliveries` mencatat `SENT` dengan `provider_message_id` nyata (Resend id / Telegram message_id / Baileys id);
4. uji retry/fallback/circuit breaker (matikan provider → FAILED → retry backoff → fallback route → SKIPPED 'another route succeeded').

## Verifikasi in-app (live hari ini)

```
SELECT channel,status,count(*) FROM notification_deliveries WHERE created_at::date='2026-08-22' GROUP BY 1,2;
IN_APP|SENT|3        ← notifikasi ops (ops-monitor event) → admin, nyata di DB
EMAIL|SENT|3         ← mock (transport=mock)
TELEGRAM|SKIPPED|3   ← route fallback (ops tanpa channel telegram ter-link)

SELECT title,type,created_at FROM notifications WHERE created_at::date='2026-08-22';
PMMI Ops: ops.event ...   (3 baris, target admin)
```

Alur outbox → materialize → delivery → in-app SENT bekerja end-to-end di produksi (worker hidup kembali sejak G2; lihat `docs/evidence/G2.md`).

## Yang dibutuhkan dari user (G3 unlock)

- Resend API key (+ verifikasi DNS domain pengirim) → EMAIL live.
- Token Telegram bot (@BotFather) + `TELEGRAM_BOT_USERNAME` → TELEGRAM live + linking.
- Nomor WhatsApp khusus PMMI (untuk Baileys pairing) atau Meta Cloud API token → WHATSAPP live.
