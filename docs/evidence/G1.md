# G1 — TLS/HTTPS + DNS 3 domain + vhost — **SELESAI**

Tanggal: 2026-08-22 (update final). Server: `pmmiserver`.

## Diagnosa awal

- DNS proxied Cloudflare, origin 530; IP publik `182.8.226.154` timeout port 80/443 (port-forward tidak aktif / CGNAT) → jalur A-record+port-forward tidak bisa; **keputusan: Cloudflare Tunnel**.

## Implementasi (jalur Cloudflare Tunnel)

1. **Edge Cloudflare** (via token user, scope Zone settings): `ssl=full`, `always_use_https=on` (redirect http→https), `min_tls_version=1.2`.
2. **Tunnel sudah ada di akun** (ternyata dibuat user sebelumnya): `pmmi-home-server` (`d65065a9-2305-4e25-8c95-6a5fbe525c14`), CNAME `*.pondokmultimedia.id → <id>.cfargotunnel.com` (proxied), ingress:
   ```
   pondokmultimedia.id → http://127.0.0.1:8080
   app.pondokmultimedia.id → http://127.0.0.1:8081
   ai.pondokmultimedia.id → http://127.0.0.1:3001
   <catch-all> → http_status:404
   ```
3. **Connector**: `cloudflared 2026.8.2` terinstal; `cloudflared service install <TUNNEL_TOKEN>` (token via `GET /accounts/<acct>/cfd_tunnel/<id>/token` — token API user scope Tunnel:Edit+DNS:Edit) → service aktif, koneksi QUIC terdaftar (sin17/sin14).

## Verifikasi (2026-08-22, dari internet)

```
curl -sI https://pondokmultimedia.id        → 200 OK   (title "Pondok Multimedia Munzalan Indonesia")
curl -sI https://app.pondokmultimedia.id    → 200 OK   (title "PMMI Digital Campus")
curl -s  https://ai.pondokmultimedia.id/health/ready → 200 {"status":"ready",...}
curl -sI http://pondokmultimedia.id         → 301 Location: https://pondokmultimedia.id/
```

Vhost nginx (`nginx/default.conf`) tetap sebagai lapisan (web :8080, dashboard :8081, api :3001); tunnel mengarah langsung ke container sesuai ingress Cloudflare.

## Dampak turunan (berfungsi sekarang)

- E2E MVP via domain produksi (G7) — 12 langkah lolos.
- Webhook Resend dapat didaftarkan: `https://ai.pondokmultimedia.id/v1/integrations/resend/webhook` (ganti `RESEND_WEBHOOK_SECRET` di .env dengan `whsec_…` dari dashboard Resend).
- CORS origin `https://pondokmultimedia.id, https://app.pondokmultimedia.id` valid (allowlist produksi).
