import React,{useEffect,useState} from 'react';
import { useParams } from 'react-router-dom';
const API=(import.meta as any).env.VITE_API_URL??'http://localhost:3001';
export default function PortfolioDetailPage(){const{slug}=useParams();const[d,setD]=useState<any>(null);const[error,setError]=useState('');useEffect(()=>{fetch(`${API}/v1/portfolio/${slug}`).then(async r=>{const x=await r.json();if(!r.ok)throw new Error(x.error??'Portfolio tidak ditemukan');return x}).then(setD).catch(e=>setError(e.message))},[slug]);return <main style={{minHeight:'100vh',background:'#09090b',color:'white',padding:'120px 24px 60px',fontFamily:'Inter,system-ui'}}><article style={{maxWidth:850,margin:'auto'}}>{error&&<h1>{error}</h1>}{d&&<><div style={{color:'#e879f9'}}>Karya {d.student_name}</div><h1 style={{fontSize:'clamp(34px,6vw,70px)',margin:'10px 0'}}>{d.title}</h1><p style={{fontSize:20,color:'#d4d4d8'}}>{d.summary}</p><div style={{display:'grid',gap:12,marginTop:28}}>{d.assets?.map((a:any)=><div key={a.id} style={{padding:16,border:'1px solid #3f3f46',borderRadius:14,background:'#18181b'}}><strong>{a.asset_type}</strong>{a.alt_text&&<p>{a.alt_text}</p>}{a.url&&<a style={{color:'#e879f9'}} href={a.url} target="_blank" rel="noreferrer">Buka asset</a>}</div>)}</div></>}</article></main>}
