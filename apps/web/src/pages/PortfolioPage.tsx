import React,{useEffect,useState} from 'react';
import { Link } from 'react-router-dom';
const API=(import.meta as any).env.VITE_API_URL??'http://localhost:3001';
export default function PortfolioPage(){const[items,setItems]=useState<any[]>([]);useEffect(()=>{fetch(`${API}/v1/portfolio`).then(r=>r.json()).then(d=>setItems(d.items??[]))},[]);return <main style={{minHeight:'100vh',background:'#09090b',color:'white',padding:'120px 24px 60px',fontFamily:'Inter,system-ui'}}><div style={{maxWidth:1100,margin:'auto'}}><h1>Portfolio Santri PMMI</h1><p style={{color:'#a1a1aa'}}>Karya pilihan yang telah dinilai dan di-feature oleh ustadz/admin PMMI.</p><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(260px,1fr))',gap:16,marginTop:28}}>{items.map(p=><Link key={p.id} to={`/portfolio/${p.slug}`} style={{textDecoration:'none',color:'white',background:'#18181b',border:'1px solid #3f3f46',padding:20,borderRadius:16}}><h2>{p.title}</h2><p>{p.summary}</p><div style={{color:'#a1a1aa'}}>{p.student_name}</div></Link>)}</div></div></main>}
