import { hostname } from 'node:os';
const req=(n:string)=>{const v=process.env[n];if(!v)throw new Error(`${n} is required`);return v};
const opt=(n:string,d?:string)=>process.env[n]??d;
export const cfg={DATABASE_URL:req('DATABASE_URL'),INSTANCE_ID:opt('WORKER_INSTANCE_ID',`${hostname()}-${process.pid}`)!,POLL_MS:Number(opt('WORKER_INTERVAL_MS','2000')),NOTIFICATION_MODE:opt('NOTIFICATION_MODE','real') as 'real'|'mock',RESEND_API_KEY:opt('RESEND_API_KEY'),RESEND_FROM_EMAIL:opt('RESEND_FROM_EMAIL','PMMI <noreply@pondokmultimedia.id>')!,TELEGRAM_BOT_TOKEN:opt('TELEGRAM_BOT_TOKEN'),WHATSAPP_PROVIDER_URL:opt('WHATSAPP_PROVIDER_URL'),WHATSAPP_PROVIDER_TOKEN:opt('WHATSAPP_PROVIDER_TOKEN'),HERMES_MODE:opt('HERMES_EXECUTION_MODE','disabled') as 'real'|'mock'|'disabled',HERMES_BINARY:opt('HERMES_BINARY','hermes')!,HERMES_TEMPLATE_PROFILE:opt('HERMES_TEMPLATE_PROFILE'),HERMES_DEFAULT_MODEL:opt('HERMES_DEFAULT_MODEL'),PMMI_AI_GATEWAY_URL:opt('PMMI_AI_GATEWAY_URL','http://127.0.0.1:3001/v1')!};
